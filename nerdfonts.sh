#!/usr/bin/env bash
# Get latest release from Github
# Download a list in a for loop based on font's name
# Extract the fonts from the compressed archive
# Move the resulting directory to $HOME/.local/share/fonts
