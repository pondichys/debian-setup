#!/usr/bin/env bash

echo "Installing curated list of external repositories"
sudo apt-get install extrepo
echo "You can now use extrepo enable {repository_name} to enable external repositories like Librewolf, Brave, VS Code, etc ..."
